// 多模块根工程。公共配置下沉到各子模块, 避免在根处混入只属于某一 loader 的插件:
//   common = 零-MC 纯算法核心 (scheduler/state/worker/diagnostic + CapturedSnapshot), 纯 java-library, 不依赖 Minecraft 或任何 loader。
//   forge  = 1.20.1 ForgeGradle + MC 胶水 + mixin; 经 source-merge 把 common 源重编进自身 reobf jar, 产出单一 mod。
// 后续 NeoForge 1.21.1 端口作为第三个 source-merge 同一 common 的子模块加入 (Stage C)。
